# Изменения относительно апстрима

Журнал наших отличий от [throneproj/Throne](https://github.com/throneproj/Throne) (тег `1.2.4`, коммит `33777e27b77cb62ef8c98a47d8ea4601de0c1983` — см. `PROISHOZHDENIE.md`). Каждая правка — с объяснением, зачем она сделана. Правило форка: минимальная дивергенция, чтобы мержить обновления апстрима как можно дольше.

Журнал ведётся по заданиям. Задание 02 не трогало код приложения вовсе (только `.github/workflows/build.yml`); задание 03 — ребрендинг — впервые вошло в C++/Qt, CMake, установщик и ресурсы. Go-ядро не менялось ни разу.

---

## Задание 02: репозиторий и CI (8 сентября 2026)

Единственный изменённый файл: `.github/workflows/build.yml`.

### 1. Матрица `build-go` обрезана с 9 целей до 2

**Было:** windows/amd64, windows/arm64, linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, darwinlegacy/amd64, windowslegacy/amd64, windowslegacy/386.

**Стало:** только `windows/amd64` и `windowslegacy/amd64`.

**Зачем:** задание 02 требует собирать только `windows64` и `windowslegacy64`, остальные платформы (macOS, Linux, Windows ARM64, Windows 32-бит) не нужны на этом шаге и не должны жечь минуты CI. Условные шаги (`Clone cronet-go`, `Download Chromium toolchain` и т.д.), которые относятся к убранным целям, не удалялись — они и так не выполняются, потому что в матрице больше нет подходящих под их `if`-условия строк. Это меньше диффа, чем вычищать код построчно, и ближе к апстриму для будущего слияния.

### 2. Матрица `build-cpp` обрезана с 11 целей до 2

**Было:** windows64, windows-arm64, linux-amd64 (2 варианта), linux-arm64 (2 варианта), darwin-amd64, darwin-arm64, darwinlegacy-amd64, windowslegacy-amd64, windowslegacy-386.

**Стало:** только `windows64` (Qt 6.11.1, x64) и `windowslegacy64` (Qt 6.2.13, x64).

**Зачем:** то же самое — задание требует только эти две цели.

### 3. Добавлена проверка SHA-256 после каждой внешней бинарной загрузки

Затронуты три загрузки: `go.zip` (легаси Go-тулчейн, job `build-go`), `qt.7z` и `openssl.7z` (job `build-cpp`, шаг «Windows - Download Qt and OpenSSL»). После каждого `curl` добавлена строка вида:

```bash
echo "<ожидаемый-sha256>  <имя-файла>" | sha256sum -c -
```

Ожидаемые хеши вынесены в `env:` на уровне job (`GO_ZIP_SHA256`, `OPENSSL_X64_SHA256`) и в поле `qt_sha256` матрицы `build-cpp` (у каждой Qt-версии свой файл и свой хеш). Значения хешей и то, как они были получены, задокументированы в `ZAVISIMOSTI.md`.

**Зачем:** отчёт 01 и решение 05 зафиксировали риск — сборка Windows тянет чужие бинарники по URL, которые мы не контролируем. Если содержимое по этим URL подменят (или репозиторий-источник исчезнет и появится с другим содержимым под тем же именем), раньше сборка молча собрала бы клиент на изменённом Qt/OpenSSL/Go-тулчейне. Теперь она вместо этого падает на шаге загрузки с понятной ошибкой `sha256sum: WARNING: 1 computed checksum did NOT match`.

### 4. `srslist.h` закреплён на коммит вместо ветки `rule-set`

**Было:** `curl ... https://raw.githubusercontent.com/throneproj/routeprofiles/rule-set/srslist.h` (три места — в шагах сборки Windows, Linux и macOS).

**Стало:** URL указывает на конкретный коммит (`${SRSLIST_COMMIT}` = `e670939edad51a9381c4577b4c05c2a76d82a41f`), плюс после загрузки — проверка SHA-256 (`${SRSLIST_SHA256}`).

**Зачем:** ветка `rule-set` перезаписывается целиком при каждом обновлении (не накапливает историю, каждый коммит — без родителя), то есть её содержимое непредсказуемо меняется под нами прямо во время сборки. Коммит по конкретному SHA неизменяем, пока он существует в репозитории. Подробности и оговорка о том, что этот коммит со временем может стать недостижимым (см. `ZAVISIMOSTI.md`, примечание 2) — в `ZAVISIMOSTI.md`.

Правка применена во всех трёх местах ради единообразия, хотя сейчас реально выполняется только шаг Windows — шаги Linux и macOS не запускаются, так как соответствующих целей больше нет в матрице.

### 5. `updater.exe` исключён из финального пакета

Добавлен новый шаг `ARSMAG - Remove updater from package` перед шагом `Tar files` в job `build-cpp`:

```bash
rm -fv "$DEST/updater.exe" "$DEST/updater" 2>/dev/null || true
```

**Зачем:** апстримный `updater.exe` при запуске «Check For Update» обращается к `https://api.github.com/repos/throneproj/Throne/releases` и предложит пользователю поставить апстримный Throne поверх нашей сборки — недопустимо для отдельного продукта. В апстримном коде уже есть проверка `!QFile::exists(".../updater.exe")`, которая сама отключает пункт меню «Check For Update», если файла нет рядом с приложением (`src/ui/mainWindow/mainwindow_setup.cpp:379-382` — путь актуален на тег 1.2.4). Поэтому решение — не класть файл в пакет, а не патчить C++/Go-код. Ноль правок в коде приложения, ноль дополнительной дивергенции. Сам Go-код по-прежнему скачивает `updater.exe` на этапе `build-go` (это отдельный, более ранний артефакт) — правка вычищает его только из финального упакованного архива на этапе `build-cpp`.

Свой канал обновлений — отдельная будущая задача (нужен домен и подписанные релизы).

### 6. Job `publish` убран целиком

**Было:** job `publish` (упаковка NSIS-инсталлятора, `.deb`-пакетов, zip-архивов под каждую платформу, публикация GitHub Release).

**Стало:** job отсутствует.

**Зачем:** задание 02 явно требует отключить публикацию релиза — на этом шаге нужны просто скачиваемые артефакты сборки, а не публичный релиз. Полностью убрать job было проще и честнее, чем оставлять его в нерабочем виде: `publish` ссылался на артефакты платформ (`linux-amd64`, `darwin-arm64` и т.д.), которых после обрезки матриц (правки 1-2) больше не существует — job бы просто падал на шаге `Pack`, если бы его оставили.

Артефакты сборки по-прежнему доступны — их публикует каждый job `build-cpp` под именем `Throne-<sha>-<dest_suffix>` (шаг `Uploading Artifact`), их можно скачать со страницы запуска workflow во вкладке Actions без дополнительной публикации релиза.

### 7. Входной параметр `publish` убран из `workflow_dispatch`

**Было:** входы `tag` и `publish` («r»/«p»/пусто).

**Стало:** только `tag`.

**Зачем:** `publish` управлял поведением job `publish`, которого больше нет (правка 6) — оставлять неиспользуемый вход было бы вводящим в заблуждение.

### 8. Комментарий-заголовок в начале файла

Добавлен блок комментариев в самом начале `build.yml`, поясняющий, что файл модифицирован, и куда смотреть за подробностями (этот файл и отчёт задания 02).

**Зачем:** человек, открывший файл без контекста (в том числе мы сами через полгода), должен сразу понять, что это не чистый апстрим.

---

## Внеочередная задача: автозапуск и минимальная публикация (9 сентября 2026)

Цель — свести ручную работу владельца к минимуму: сборка должна стартовать сама по `push` в `main`, а готовые архивы — быть доступны без входа в GitHub. Изменён только `.github/workflows/build.yml`.

### 9. Добавлен запуск по `push` в `main`, `tag` стал необязательным входом

**Было:** только `workflow_dispatch`, вход `tag` — `required: true`. Запуск возможен только вручную через веб-интерфейс Actions.

**Стало:**

```yaml
on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      tag:
        description: "Release Tag and version"
        required: false
        default: "0.0.0-dev"
```

и в job `build-cpp`:

```yaml
INPUT_VERSION: ${{ github.event.inputs.tag || format('0.0.0-ci.{0}', github.run_number) }}
```

**Зачем:** до этой правки владельцу нужно было каждый раз вручную открывать вкладку Actions, нажимать «Run workflow» и вписывать тег. При событии `push` объект `github.event.inputs` не существует вовсе (это поле есть только у `workflow_dispatch`), поэтому `github.event.inputs.tag` при push молча превращается в пустую строку — раньше это ничего не ломало напрямую (`INPUT_VERSION` просто был бы пустым), но подстановка версии в собранный exe была бы бессмысленной. Выражение `||` подставляет `0.0.0-ci.<номер_запуска>` в этом случае, а `default` на самом входе — для ручного запуска через UI, если поле оставить пустым. Обычный ручной `workflow_dispatch` с явно указанным тегом продолжает работать как раньше.

### 10. Job публикации релиза возвращён в минимальном виде

**Было (после задания 02):** публикации нет вообще, только скачивание артефактов Actions со страницы запуска (нужна авторизация в GitHub).

**Стало:** новый job `publish-release`, `needs: [build-cpp]`, `permissions: contents: write`. Скачивает оба Actions-артефакта (`Throne-<sha>-windows-amd64`, `Throne-<sha>-windowslegacy-amd64`), распаковывает `artifacts.tgz`, перепаковывает содержимое папок `deployment/windows-amd64` и `deployment/windowslegacy-amd64` в `windows64.zip` и `windowslegacy64.zip`, публикует их как prerelease с тегом `ci-<github.run_number>` через `gh release create` со встроенным `GITHUB_TOKEN` (`secrets.GITHUB_TOKEN`, выдаётся Actions автоматически, ничего от владельца не требуется).

**Зачем:** ассеты релиза (в отличие от артефактов Actions) в публичном репозитории скачиваются анонимно, по прямой ссылке, без входа в GitHub — это позволяет забирать и проверять сборку программно, не трогая веб-интерфейс. Это **не** возврат апстримного job `publish`, убранного в задании 02 (правка 6 выше): апстримный job собирал NSIS-инсталлятор и пакеты под все платформы через `ghr`; здесь — только перепаковка уже собранных архивов под две нужные цели, без инсталлятора и без сторонних `actions/*` для релиза (использован `gh` CLI, предустановленный на runner'ах GitHub, вместо стороннего action — меньше внешних зависимостей). Тег `ci-<номер>` и пометка `prerelease` явно отделяют эти сборки от будущих настоящих релизов для пользователей.

**Оговорка:** каждый push в `main` создаёт отдельный prerelease (теги не переиспользуются, `github.run_number` растёт монотонно) — со временем в разделе Releases накопится история CI-сборок. Это сознательно оставлено как есть на этом шаге; ротацию/удаление старых prerelease можно добавить отдельной правкой, если это станет проблемой.

---

## Исправление: дублирующиеся ключи `env:` ломали весь workflow (9 сентября 2026)

Первый же запуск (по `push`, после правок из предыдущего раздела) упал мгновенно, `conclusion: failure`, ни один job не стартовал — `build-go` и `build-cpp` не появились вовсе. Причина нашлась при разборе с владельцем: в обоих job'ах секция `env:` была объявлена **дважды на одном уровне вложенности** — правки задания 02 добавили свой блок `env:` (с `GO_ZIP_SHA256` в `build-go`; с `OPENSSL_X64_SHA256`/`SRSLIST_COMMIT`/`SRSLIST_SHA256` в `build-cpp`), не заметив, что чуть ниже в том же job'е уже был апстримный блок `env:` (с `DEST` в `build-go`; с `INPUT_VERSION`/`DEST`/`DEST_SUFFIX`/`ARCH`/`LAUNCHER_ARGS` в `build-cpp`).

Два ключа `env` в одной YAML-мэппинге — не «последний побеждает», как молча делает большинство парсеров (в т.ч. PyYAML, которым это проверялось перед коммитом и который ошибку не показал), а прямая ошибка по строгой YAML-схеме, которую использует парсер GitHub Actions. Он отвергает файл целиком, ещё до того как решает, какие job'ы вообще создавать — отсюда `failure` без единого job'а и без единого шага в логах.

**Исправлено:** в каждом job'е оставлена одна секция `env:`, объединяющая оба набора переменных; комментарии `ARSMAG` при слиянии сохранены на своих переменных.

**Зачем отдельная запись, а не правка задания 02 задним числом:** ошибка живёт в файле с задания 02, но обнаружилась только сейчас, когда workflow впервые реально запустили. Правило проекта — фиксировать отрицательный результат наравне с положительным, поэтому это отдельный пункт, а не тихая правка истории.

**Урок на будущее:** проверка `python -c "import yaml; yaml.safe_load(...)"` подтверждает только то, что файл — валидный YAML *в понимании PyYAML* (который по умолчанию терпим к дублирующимся ключам). Она не гарантирует, что GitHub примет файл. Перед коммитом правок в `.github/workflows/*.yml` нужно отдельно проверять дублирующиеся ключи на одном уровне вложенности (особенно `env:`, `with:`, `permissions:` — секции, которые легко добавить второй раз, не заметив уже существующую апстримную).

---

## Что не менялось в заданиях 02 и во внеочередных правках

- Все версии инструментов (CMake, MSVC, Go 1.26.5, Qt 6.11.1/6.2.13) — оставлены как в апстриме.
- Логика кэширования (`actions/cache`, `ccache`/`sccache`) — не тронута.
- Код приложения (C++, Go, `.ui`-файлы, `3rdparty/`) — не тронут.
- `script/*.sh`, `script/windows_installer.nsi`, `CMakeLists.txt` — не тронуты.
- Оригинальный `LICENSE` и копирайты — не тронуты.

---

## Задание 03: ребрендинг Throne → ArsLink (14 сентября 2026)

Основание — решение 06 «Что переименовываем, а что обязаны сохранить» в проектной документации клиента. Разделение простое: **бренд** — то, что видит пользователь, здесь имени Throne не остаётся; **атрибуция** — указание, чей код лежит в основе, она сохраняется полностью и намеренно.

Затронуто 47 файлов, из них 8 — бинарные ресурсы (иконки).

### 11. Имя приложения в Qt

`src/main.cpp`: `QApplication::setApplicationName("Throne")` → `"ArsLink"`.

**Зачем:** это имя определяет не только то, что показывает Qt, но и путь к каталогу конфигурации через `QStandardPaths::AppConfigLocation`. **Известное следствие:** настройки, лежавшие по старому пути, новой сборкой не подхватываются. Пользователей у продукта пока нет, поэтому терять нечего; после первого публичного релиза такая правка потребовала бы миграции.

### 12. Имя в интерфейсе: заголовок окна, трей, заголовки диалогов

`src/ui/mainWindow/mainwindow_setup.cpp`: `software_name = "Throne"` → `"ArsLink"`.

**Зачем:** одна переменная закрывает почти весь видимый слой сразу — она подставляется в `make_title()` (`mainwindow_view.cpp`), то есть в заголовок главного окна и в тултип иконки в трее, и в заголовок каждого `QMessageBox` приложения. Именно поэтому `make_title` править не пришлось: правка в одной точке вместо десятка — ровно то, чего требует правило форка.

Дополнительно `include/ui/mainwindow.ui` — статический `windowTitle` (виден до первой перерисовки заголовка) и `include/ui/stats/dialog_runtime_stats.ui` вместе с `dialog_runtime_stats.cpp` — подпись графика в панели Runtime Stats.

### 13. Метаданные исполняемого файла

`cmake/windows/windows.cmake`:

| Поле | Значение |
|---|---|
| `NAME`, `BUNDLE`, `COMPANY_NAME`, `FILE_DESCRIPTION` | `ArsLink` |
| `COMPANY_COPYRIGHT` (→ `LegalCopyright`) | `Copyright (C) 2026 ArsLink. Based on Throne, Copyright (C) Throne contributors. Licensed under GPL-3.0.` |
| `COMMENTS` (→ `Comments`) | `Based on Throne (https://github.com/throneproj/Throne), GPL-3.0.` |

**Зачем именно так:** поля, которыми Windows *идентифицирует продукт* (`ProductName`, `FileDescription` — его показывает Диспетчер задач, `InternalName`, `OriginalFilename`, `CompanyName`), имени Throne не содержат. Атрибуция вынесена в `LegalCopyright` и `Comments` — это её штатное место в формате VERSIONINFO, требование GPL-3.0 и одновременно наш аргумент при модерации. Строки только ASCII: ресурс собирается `rc.exe`, и не-ASCII в узких строковых литералах здесь ненадёжен.

### 14. Имя таргета и выходного файла C++-части

`CMakeLists.txt`: `project(Throne)` → `project(ArsLink)`, `qt_add_executable(Throne …)` → `ArsLink`, и все последующие ссылки на таргет (`set_property`, `target_sources`, `target_compile_definitions`, `target_link_libraries`, `qt_add_lupdate`, `qt_add_lrelease`, `qt_finalize_executable`). Итог: `build/ArsLink.exe` вместо `build/Throne.exe`.

Следом обновлён `script/deploy_windows.sh` (копирует `ArsLink.exe` и `ArsLink.pdb`). Идентификатор macOS-бандла `moe.Throne.macosx` → `am.arslink.macosx`; в `res/MacOSXBundleInfo.plist` — то же плюс описание типа документа.

### 15. Имя бинарника ядра — переименование на этапе упаковки

`script/build_go.sh`: после `go build` добавлено переименование `ThroneCore[.exe]` → `ArsLinkCore[.exe]`.

**Зачем на этом этапе, а не в `go.mod`:** `go build -o <каталог>` называет файл по имени модуля, а имя модуля `ThroneCore` — идентификатор во всех импортах внутри ядра. Его переименование задело бы десятки файлов и конфликтовало бы при каждом слиянии с апстримом, не дав пользователю ничего (решение 06). Переименование сделано в одной точке и работает сразу для всех платформ.

Согласованно поправлены все, кто ищет ядро по имени:

- `src/global/Configs.cpp` — `FindCoreRealPath()`;
- `src/ui/mainWindow/mainwindow_setup.cpp` — путь запуска ядра;
- `src/sys/windows/MiniDump.cpp` — регистрация ядра в WER LocalDumps (иначе крэш-дампы ядра перестали бы собираться);
- `script/windows_installer.nsi`, `script/deploy_linux64_system_qt.sh`.

**Осторожно при слиянии с апстримом:** это единственное место, где мы полагаемся на то, что `go build` кладёт файл с предсказуемым именем. Если апстрим переименует модуль или сменит форму `-o`, шаг `mv` упадёт — и это правильно: лучше падение сборки, чем молча уехавшее имя.

### 16. Установщик NSIS

`script/windows_installer.nsi` переписан по всем видимым точкам: `Name`, `OutFile` → `ArsLinkSetup.exe`, `APP_DIR_NAME` → `ArsLink` (он же задаёт пути установки по умолчанию `$LOCALAPPDATA\ArsLink` и `$PROGRAMFILES64\ArsLink`), тексты страниц приветствия, выбора каталога, завершения и удаления данных, имена ярлыков `$DESKTOP\ArsLink.lnk` и `$SMPROGRAMS\ArsLink.lnk`, имена файлов при распаковке, ключи реестра `Software\ArsLink` и `…\Uninstall\ArsLink`, `DisplayName`.

Не тронуто: пути к файлам иконок (`res\Throne.ico`, `res\ThroneDel.ico` — см. пункт 18) и имя файла базы `throne.db` в проверке перед удалением данных (пользователю не видно, смена ломает совместимость с уже установленной версией).

**Оговорка:** установщик наш CI сейчас не собирает вовсе (job публикации только перепаковывает архивы, `makensis` не вызывается), поэтому эти правки проверены только чтением. Кроме того, NSIS-скрипт по-прежнему кладёт в пакет `updater.exe`, которого в нашей сборке нет (решение 05) — то есть в текущем виде он и не собрался бы. Это состояние досталось от задания 02 и правится тогда, когда установщик реально понадобится.

### 17. Реестр, автозапуск и системные регистрации

- `src/sys/windows/UrlScheme.cpp`: ProgId `Throne.Config` → `ArsLink.Config`, отображаемые строки `URL:Throne Protocol` → `URL:ArsLink Protocol`, `Throne profile` → `ArsLink profile`, `FriendlyAppName` → `ArsLink`. Ревизия состояния регистрации поднята `v2|` → `v3|` — это штатный механизм самого файла: он заставляет уже установленные копии перерегистрироваться, хотя путь запуска не менялся.
- `src/sys/windows/AutoRun.cpp`: имя задачи в Планировщике заданий `Throne AutoRun <hash>` → `ArsLink AutoRun <hash>`, временный файл `Throne_Task.xml` → `ArsLink_Task.xml`.
- `src/sys/linux/UrlScheme.cpp`: `Name=Throne` → `Name=ArsLink` в `.desktop`-файле.

**Не тронута схема `throne://`** (ключ `HKCU\Software\Classes\throne`, `x-scheme-handler/throne`, разбор ссылок в `mainwindow_deeplink.cpp`). Это формат ссылок, а не бренд: такие ссылки генерируют сторонние инструменты, и они уже сохранены у пользователей. Переименование схемы сломало бы импорт и требует отдельного решения — с поддержкой обеих схем.

### 18. Иконки — оригинальные заглушки, имена файлов сохранены

Заменено содержимое `res/Throne.ico`, `res/ThroneDel.ico` и `res/public/{Dns,Off,Proxy,Proxy-Dns,Throne,Tun}.png`.

**Имена файлов сохранены сознательно:** они зашиты в `include/global/Const.hpp` (`Configs::Information::iconNames`), в `res/ArsLink.qrc` и в `src/ui/setting/Icon.cpp`. Пользователю эти имена не видны, а переименование умножило бы дивергенцию ради нулевой выгоды (решение 06).

Изображение — собственного изготовления, не производное от иконки Throne: скруглённый квадрат со стилизованной геометрической буквой «A» из трёх штрихов с круглыми окончаниями. Состояние кодируется цветом плитки: серый — выключено, синий — работает, зелёный — прокси, янтарный — DNS, бирюзовый — прокси и DNS, фиолетовый — Tun; деинсталлятор — красный. Отрисовано скриптом на System.Drawing, `.ico` собраны скриптом на Python; оба скрипта одноразовые и в репозиторий не кладутся (постоянную иконку владелец закажет отдельно).

Структура `.ico` повторяет исходную по размерам записей побайтово: 8 записей — 256×256 как PNG, 64/48/40/32/24/20/16 как 32-битный BMP. Требуемые заданием 16/32/48/256 на месте; PNG трея — 512×512, квадратные.

### 19. Окно «О программе» — новое, как штатное место атрибуции

В апстриме окна «О программе» не было вообще. Добавлены пункт меню `actionAbout` в меню Tools (`include/ui/mainwindow.ui`) и сам диалог (`src/ui/mainWindow/mainwindow_setup.cpp`, рядом с обработчиком «Check For Update»).

Содержит: название и версию; указание, что продукт основан на Throne (со ссылкой на `github.com/throneproj/Throne`) под GPL-3.0 с сохранением копирайтов авторов; ссылку на наш публичный репозиторий; упоминание ядер sing-box (GPL-3.0) и Xray-core (MPL-2.0); отказ от гарантий.

**Зачем новое окно, а не строка где-нибудь:** атрибуция обязательна по GPL, и ей нужно предсказуемое место, которое найдёт и пользователь, и модератор магазина. Диалог собран кодом, без нового `.ui`-файла — так правка ограничена двумя файлами апстрима. Текст сознательно не переводится: это юридическая часть, она держится языка `LICENSE`.

### 20. Прочие видимые пользователю строки

- `src/database/SettingsRepo.cpp` — User-Agent по умолчанию `Throne/<версия>` → `ArsLink/<версия>`. Он уходит на серверы подписок, то есть виден третьим лицам.
- `src/main.cpp` — предупреждение о некорректном завершении прошлого сеанса.
- `src/sys/windows/MiniDump.cpp` — заголовок крэш-отчёта, текст и заголовок окна об аварийном завершении, имена файлов дампов `Throne_*` → `ArsLink_*`.
- `src/global/Logger.cpp` — маркеры начала и конца сеанса в журнале.
- `src/ui/setting/dialog_basic_settings.cpp` — резервные копии: имя файла по умолчанию, фильтр в диалоге, сообщения о восстановлении. Расширение `.thrbackup` сохранено: слова Throne оно не содержит и завязано на формат, а не на бренд.
- `src/ui/setting/dialog_manage_routes.cpp`, `src/database/entities/RouteProfile.cpp` — подсказка и сообщение об ошибке про ссылку маршрута.
- `src/ui/profile/edit_custom.cpp`, `src/ui/setting/RawRouteItem.cpp`, `include/global/Const.hpp` (`HijackInfo`) — пояснительные тексты.
- `src/ui/mainWindow/mainwindow_system.cpp` — «Please run ArsLink as admin».
- `res/dashboard-notice.html` — путь к каталогу clash-дашборда (каталог установки теперь `ArsLink`).
- `res/translations/{ru_RU,fa_IR,zh_CN}.ts` — исходные строки приведены в соответствие с изменённым кодом, иначе переводы отвалились бы на всех этих сообщениях и они показывались бы по-английски. Русские переводы, где имя встречалось внутри самого перевода, тоже обновлены.

### 21. Инфраструктура репозитория

- `res/Throne.qrc` → `res/ArsLink.qrc` (плюс ссылки в `CMakeLists.txt` и в трёх `.ui`-файлах). Внутренние префиксы ресурсов (`:/Throne/…`) оставлены как есть — они не видны пользователю, а их смена задела бы `Icon.cpp` восемью строками.
- `.github/workflows/build.yml` — имя workflow и имена артефактов `Throne-<sha>-…` → `ArsLink-<sha>-…` (согласованно во всех пяти местах, включая job публикации).
- `.github/ISSUE_TEMPLATE/{bug_report,routing_issue}.yml` — тексты форм: это формы нашего публичного репозитория.
- `.gitignore` — игнорируемый бинарник `/Throne` → `/ArsLink`.
- `script/deploy_linux64.sh`, `deploy_linux64_system_qt.sh`, `deploy_macos.sh`, `pack_debian.sh` — имя бинарника вслед за переименованием таргета. Эти цели наш CI не собирает; правка сделана, чтобы не оставлять заведомо сломанные скрипты. **Сборкой не проверены.**

### Что в задании 03 сознательно НЕ менялось

- **Атрибуция.** `LICENSE`, копирайт-заголовки внутри исходных файлов, `PROISHOZHDENIE.md` — не тронуты. Это не формальность: именно они отличают легальный форк от перепакованного клона.
- **Имя Go-модуля `ThroneCore`** в `core/server/go.mod` и весь код ядра — см. пункт 15.
- **Имя файла базы `throne.db`** — пользователю не видно, смена ломает совместимость.
- **Схема ссылок `throne://`** — см. пункт 17.
- **Имена файлов иконок в `res/`** — см. пункт 18.
- **Внутренние идентификаторы**: пространство имён `Throne::` (`PeriodicRunner`), макрос `THRONE_ARCH`, имена виджетов `labelThroneName` / `vThroneCpu` / `vThroneRam`, константа `kRuntimeThroneColor`, префиксы ресурсов `:/Throne/`. Пользователь их не видит; каждый переименованный идентификатор — конфликт при каждом будущем слиянии.
- **Комментарии в исходниках апстрима**, где упоминается Throne как проект-первоисточник. Правило применено единообразно: комментарии не трогаем.
- **Механизм автообновления.** `src/ui/mainWindow/mainwindow_system.cpp` по-прежнему обращается к `api.github.com/repos/throneproj/Throne/releases`. Код недостижим в нашей сборке: пункт меню «Check For Update» отключается, если рядом с приложением нет `updater.exe`, а мы его в пакет не кладём (решение 05, правка 5). Свой канал обновлений — отдельная задача.
- **`script/install_linux.py`** — установщик для Linux, который скачивает релизы из `throneproj/Throne`. Не адаптирован намеренно: переименовать в нём строки, оставив источник апстримным, значило бы выдавать чужие сборки за наши. Наша Linux-дистрибуция — отдельная задача.
- **`README.md`** — остался апстримным. Это публичное лицо репозитория, и его текст (позиционирование, отсутствие слова «VPN», описание продукта) — решение владельца, а не механическая замена имени.

---

## Задание 04: README репозитория (14 сентября 2026)

Единственный изменённый файл: `README.md`.

### 22. Апстримный README заменён на наш

**Было:** README проекта Throne — заголовок «Throne (Formerly Nekoray)», скриншот их интерфейса, список протоколов, инструкции по установке из релизов `throneproj/Throne`, ссылка на стороннее RPM-репо, раздел FAQ про отличия от Nekoray.

**Стало:** короткий README ArsLink: что это за программа, что она делает, поддерживаемые платформы, приватность, происхождение и лицензия.

**Зачем:** это последнее место, где имя Throne оставалось на виду как название нашего продукта — и самое заметное, потому что README открывается первым при заходе в репозиторий. Оставлять его апстримным после ребрендинга (задание 03) было нельзя.

Текст пришёл готовым в задании 04 и размещён дословно, без правок.

Существенное в нём:

- **Прямо сказано, что серверов и подписок мы не предоставляем.** Это требование 1 из правил проекта («приложение не привязано к сервису») в явной формулировке — и одновременно первое, что должен увидеть модератор магазина.
- **Слова «VPN» нет.** Позиционирование — утилита для подключения к собственной сетевой инфраструктуре (требование 2).
- **Раздел Privacy** фиксирует отсутствие телеметрии (требование 3).
- **Раздел Built on** — атрибуция: форк Throne под GPL-3.0, продолжающий архивированный NekoRay, с сохранением копирайтов авторов, плюс ссылки на `IZMENENIYA.md` и `PROISHOZHDENIE.md`. Это та же атрибуция, что в окне «О программе» и в `LegalCopyright` (задание 03, пункты 13 и 19), только развёрнутая.
- **Апстримный скриншот удалён** — на нём интерфейс Throne с их брендом. Своих скриншотов пока нет: их добавят, когда будет постоянная иконка и финальный вид окна.

**Проверено перед коммитом:** все относительные ссылки резолвятся (`IZMENENIYA.md`, `PROISHOZHDENIE.md`, `LICENSE` лежат в корне); внешние ссылки открываются, включая `MatsuriDayo/nekoray` (существует, архивирован 17 марта 2025 — формулировка «archived» верна) и `arslink-dev/arslink-desktop` (публичный, раздел Releases есть); изображений в файле не осталось ни одного; слова «VPN», имени сервиса и его адресов в тексте нет.

**Расхождение с текущим состоянием CI** — см. отчёт 03, дополнение по заданию 04: README обещает контрольную сумму SHA-256 к каждому релизу, а наш workflow её сейчас не публикует.
